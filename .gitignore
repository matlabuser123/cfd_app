build/
.vs/
.vscode/*.log
*.user
*.suo

# Build-generated install staging mirror (regenerated by scripts/package-release.ps1)
package/staging/

# Release archives are large binaries better attached to a GitHub Release than kept in history;
# the evidence text/json/csv alongside them under results/release/<version>/ is still tracked.
results/release/*/*.zip
